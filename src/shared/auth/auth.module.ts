/**
 * Shared authentication module stub.
 *
 * Full implementation in Sprint 2. Will contain:
 * - AuthService (login, logout, refresh, 2FA)
 * - AuthController (POST /auth/login, etc.)
 * - JwtStrategy (Passport)
 * - TotpService
 * - RefreshToken entity/repository
 */
export class AuthModule {}
