/**
 * AWS SES email dispatch module stub.
 *
 * Full implementation:
 * - SES v2 client for transactional email
 * - HTML + plain-text multipart emails
 * - Template rendering (Handlebars)
 * - Email dispatch via BullMQ `email` queue for async sending
 */
export class MailerModule {}
