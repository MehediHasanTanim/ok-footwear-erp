/**
 * SSL Wireless SMS dispatch module stub.
 *
 * Full implementation:
 * - SSL Wireless API v3 client (Bangladesh SMS gateway)
 * - Single/bulk SMS dispatch
 * - Delivery status webhook handler
 * - SMS dispatch via BullMQ `sms` queue for async sending
 */
export class SmsModule {}
